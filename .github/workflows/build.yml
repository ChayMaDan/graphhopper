name: Build and Test
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java-version: [ 24, 25-ea ]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java-version }}
          distribution: temurin
      - name: Cache Maven artifacts
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - name: Cache node
        uses: actions/cache@v4
        with:
          path: web-bundle/node
          key: ${{ runner.os }}-node-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os}}-node-
      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: web-bundle/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/pom.xml', '**/package.json') }}
          restore-keys: |
            ${{ runner.os}}-node_modules-
      #Build et Test
      - name: Build and Test ${{ matrix.java-version }}
        id: tests
        continue-on-error: true
        run: | 
          mvn -B clean test
          TEST_OUTPUT=$?
          echo "test_result=$TEST_OUTPUT" >> $GITHUB_OUTPUT
          exit 0 
      #Rickroll
      - name: Rickroll
        if: steps.tests.outputs.test_result != '0'
        run: |
          echo ""
          echo "###################################################"
          echo ""
          echo ""
          echo "                           ************"
          echo "                           *********"
          echo "                           ************"
          echo "                           *********"
          echo "                           *"
          echo "                           *"
          echo "                           *"
          echo "                         * * *"
          echo "                        ** * **"
          echo "                      **** * ****"
          echo "                    ****** * ******"
          echo "                  ******** * ********"
          echo "                ********** * **********"
          echo "              ************ * ************"
          echo "            ************** * **************"
          echo "  *************************************************"
          echo "      *****************************************"
          echo "          *********************************"
          echo "            *****************************"
          echo "                *********************"
          echo "###################################################"
          echo "###################################################"
          echo ""
          echo ""
          echo "    OHÉ!! OHÉ!! MATELOT !! Matelot navigue sur les flots..."
          echo ""
          echo "                Vos tests ont échoué !  :-("
          echo "         Corrigez votre code avant de vous noyer !!!!! "
          echo ""

          echo "::error title= Rickroll Test Failure:: Vos tests ont échoué!"
          echo "::warning title= Meme::https://blog.zegocloud.com/wp-content/uploads/2024/02/programming-meme-22.jpg"
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ![MEME](https://blog.zegocloud.com/wp-content/uploads/2024/02/programming-meme-22.jpg)

          Un ou plusieurs tests ont échoué ! Corrigez votre code. 
          EOF
      #Build doit échoué par la suite
      - name: Build failure when tests fail
        if: steps.tests.outputs.test_result != '0'
        run: |
          echo "Compilation échouée : échecs de tests"
          exit 1

